# config 폴더 안내

`config/` 폴더는 코드 스타일 / 빌드 도구 설정 파일 등을 모아두는 공간입니다.

예시:

- `config/checkstyle/`
  - Java 코드 스타일 규칙 정의 (예: `checkstyle.xml`)
  - Gradle 또는 IDE에서 이 규칙을 사용해서 코드 스타일 검사 수행

팀원들과 협업할 때 “어떤 스타일로 코드를 작성할지”에 대한 기준을 명시하는 용도로 사용합니다.
