# domain

> 여러 도메인에서 공유하는 **공용 도메인 타입/enum/상수**를 모아두는 패키지입니다.

## 예시

- 채용 관련 경험 레벨 enum (`ExperienceLevel` 등)
- 공통 상태값, 타입 코드 등

## 규칙

- 실제 DB 엔티티(테이블 매핑)는 각 기능 패키지(`resume`, `section`, `jobs`, `user` 등)에 두는 것을 우선합니다.
- 이 패키지에는 **여러 도메인에서 동시에 사용하는 공용 enum/상수**만 배치합니다.
